<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= csrfToken %>">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.0.2/dist/tailwind.min.css" rel="stylesheet">
    <title>Create Chapter</title>
    <style>
        body {
            background: linear-gradient(135deg, #4CAF50, #2196F3);
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .header {
            background: linear-gradient(135deg, #4CAF50, #2196F3);
            color: white;
            padding: 20px;
            border-radius: 10px 10px 10px 10px;
            text-align: center;
            display: flex;
            justify-content: center;
        }

        .form-container {
            background: #fff;
            padding: 2rem;
            border-radius: 1rem;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.2);
        }

        .form-title {
            text-align: center;
            font-size: 1.25rem;
            font-weight: 600;
            color: #4a90e2;
            margin-bottom: 1rem;
        }

        .form-label {
            font-size: 0.875rem;
            color: #4a90e2;
        }

        .form-input {
            width: 100%;
            padding: 0.5rem;
            border: 1px solid #e2e8f0;
            border-radius: 0.25rem;
            outline: none;
            transition: border-color 0.3s;
        }

        .form-input:focus {
            border-color: #4299e1;
        }

        .form-textarea {
            width: 100%;
            padding: 0.5rem;
            border: 1px solid #e2e8f0;
            border-radius: 0.25rem;
            outline: none;
            resize: vertical;
        }

        .form-button {
            width:100%;
            padding: 0.5rem 0;
            background-color: #0cc71e;
            color: #fff;
            font-weight: 500;
            border: none;
            border-radius: 0.25rem;
            cursor: pointer;
            transition: background-color 0.3s;
            justify-content: center;
            
        }

        .form-button:hover {
            background-color: #15a308;
            border-radius: 5px 5px 5px 5px;

        }

        a {
        display: block;
        color: #000000;
        text-align: center;
        padding: 10px 12px;
        text-decoration: none;
      }
      a:hover:not(.active) {
        background-color: rgb(6, 191, 62);
        border-radius: 5px 5px 5px 5px;
      }
    </style>
</head>

<body class="bg-gray-200">
<a href="/home" style="position: absolute; font-weight: bold; color: #ffffff; top: 10px; left: 12px;">Home</a>
<a href="/createCourse" style="position: absolute; font-weight: bold; color: #ffffff; top: 10px; left: 75px;">Create Course</a>
    <div class="form-container">
        <div >
            <h1 class="header" >Create Course</h1>
    </div>
                <form action="/createCourse" class="p-6" method="POST">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <div class="mb-4">
                        <label for="title" class="form-label">Course Title</label>
                        <input type="text" id="title" name="title" placeholder="Enter the Course Title" class="form-input" required>
                    </div>
                    <button type="submit" class="form-button">Create Course</button>
                </form>
</body>
</html>


